# user-validate-directory-writable --

# this script tests if the parameter is the empty string
declare PATHNAME=${1}

user-validate-directory "${PATHNAME}" && user-pathname-writable "${PATHNAME}"

### end of file
# Local Variables:
# mode: sh
# End:
