# user-validate-file-readable --

# this script tests if the parameter is the empty string
declare PATHNAME=${1}

user-validate-file "${PATHNAME}" && user-pathname-readable "${PATHNAME}"

### end of file
# Local Variables:
# mode: sh
# End:
